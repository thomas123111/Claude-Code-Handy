name: Deploy Pfotenwelt to GitHub Pages

on:
  push:
    branches: ['claude/mobile-arena-game-DZAAt', 'main', 'claude/pfotenwelt-game-status-HxC4K']
    paths:
      - 'pfotenwelt/**'
      - '.github/workflows/deploy-game.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: pfotenwelt/package-lock.json

      - name: Install dependencies
        working-directory: pfotenwelt
        run: npm ci

      - name: Build
        working-directory: pfotenwelt
        run: npx vite build --base /Claude-Code-Handy/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pfotenwelt/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
